interface:
  display_name: "NativeUI Run"
  short_description: "Build and launch every available local host"
  default_prompt: "Use $nativeui-run to build and launch this NativeUI app on every selected locally available host."
